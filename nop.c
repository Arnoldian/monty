#include "monty.h"

/**
  *f_nop- nothing opcode
  *@head: head of stack
  *@counter: count
  *Return: no return statement
 */

void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
